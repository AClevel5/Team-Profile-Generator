function generateManager(data) {
    return `
   
    name is : ${data.name}
    id is : ${data.id}
    Email is : ${data.email}
    Office Number is : ${data.officeNumber}
        

`
}
module.exports = generateManager;
